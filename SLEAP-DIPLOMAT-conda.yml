name: sleap-diplomat
channels:
  - conda-forge
  - nvidia
  - anaconda
dependencies:
  - python=3.10
  - pytorch                   # GPU-enabled PyTorch build
  - cudatoolkit=11.7          # Adjust as needed
  - typing-extensions=3.10.0.2
  - numpy=1.21.6
  - pandas
  - matplotlib
  - scikit-learn
  - opencv
  - pip  # pip will be used later to install additional packages
